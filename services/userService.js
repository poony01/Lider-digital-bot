// services/userService.js (modo seguro para testes)
export async function verificarOuCriarUsuario(chatId, nome) {
  console.log(`🧪 Simulando cadastro do usuário: ${chatId} - ${nome}`);
  // Aqui você pode usar lógica real com banco no futuro
  return true;
}
