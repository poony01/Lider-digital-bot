export async function processarPergunta(pergunta) {
  // Aqui você conecta com a API da OpenAI, exemplo:
  // const resposta = await fetch(...);
  // return resposta.data.choices[0].text;
  return "Resposta da IA (exemplo)";
}
