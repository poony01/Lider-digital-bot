// Serviço para resposta em voz (texto para áudio).

export default {
  async textoParaVoz(texto, usuarioId) {
    // Integração futura com API de TTS (text-to-speech).
    // Retorne URL ou buffer de áudio.
    return "URL_OU_BUFFER_DE_AUDIO";
  }
};
